import "package:flutter/material.dart";

class MyColor{
  double value;
  Color color;

  MyColor(this.value, this.color);
}

class Units{
  String name;
  double value;

  Units(this.name,this.value);

}